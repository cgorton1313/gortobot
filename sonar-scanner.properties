# must be unique in a given SonarQube instance
sonar.projectKey=gortobotkey

# This is the name and version displayed in the SonarQube UI. Optional.
sonar.projectName=Gortobot
sonar.projectVersion=1.0

# Path is relative to the sonar-project.properties file. Replace "\" by "/" on Windows.
# This property is optional if sonar.modules is set.
sonar.sources=.

# Encoding of the source code. Default is default system encoding
#sonar.sourceEncoding=UTF-8

# CG notes...
# To run a one-off analysis, open cmd shell (as admin), cd to this project root, and do:
# sonar-scanner -Dsonar.projectKey=gortobotprojectkey -Dsonar.organization=cgorton1313-github -Dsonar.sources=. -Dsonar.cfamily.build-wrapper-output=bw-output -Dsonar.host.url=https://sonarcloud.io -Dsonar.login=c8fb8af7fcc125b0c93cc45a2f6b703489f7cedc

# To automate via Travis CI, see:
# https://about.sonarcloud.io/get-started/
